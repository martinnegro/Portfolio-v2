/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['i.ibb.co','i.postimg.cc','raw.githubusercontent.com'],
  },
}

module.exports = nextConfig
